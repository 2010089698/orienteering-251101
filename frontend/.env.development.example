# 開発環境で利用するイベントAPIのベースURLを指定します。
# バックエンドがローカルで稼働している場合の例:
VITE_API_BASE_URL=http://localhost:3000

# 主催者IDを指定します。バックエンドの DEFAULT_ORGANIZER_ID (organizer-default) に合わせています。
VITE_ORGANIZER_ID=organizer-default

# フロントエンドの開発サーバーポート
FRONTEND_PORT=5173

# バックエンドの開発サーバーポート
BACKEND_PORT=3000
